version: '3.8'
services:
  resume-builder:
    build: .
    container_name: resume-builder-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - '3000:3000'
